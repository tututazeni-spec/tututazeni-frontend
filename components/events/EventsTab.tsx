// components/events/EventsTab.tsx
// Aba "Eventos" (docs/events.md secção 2) — lista e gestão dos eventos.
// Placeholder: conteúdo real entra quando esta tarefa for trabalhada.

import { CalendarDays } from 'lucide-react';
import { EmptyState } from '@/components/ui/EmptyState';

export function EventsTab() {
  return (
    <EmptyState
      icon={CalendarDays}
      title="Eventos"
      description="Lista e gestão dos eventos — em construção."
    />
  );
}
