// components/events/ParticipantsTab.tsx
// Aba "Participantes" (docs/events.md secção 4) — gestão das inscrições.
// Placeholder: conteúdo real entra quando esta tarefa for trabalhada.

import { Users } from 'lucide-react';
import { EmptyState } from '@/components/ui/EmptyState';

export function ParticipantsTab() {
  return (
    <EmptyState
      icon={Users}
      title="Participantes"
      description="Gestão das inscrições — em construção."
    />
  );
}
