// components/events/ReportsTab.tsx
// Aba "Relatórios" (docs/events.md secção 11) — indicadores e análise dos
// eventos. Placeholder: conteúdo real entra quando esta tarefa for
// trabalhada.

import { BarChart3 } from 'lucide-react';
import { EmptyState } from '@/components/ui/EmptyState';

export function ReportsTab() {
  return (
    <EmptyState
      icon={BarChart3}
      title="Relatórios"
      description="Indicadores e análise dos eventos — em construção."
    />
  );
}
