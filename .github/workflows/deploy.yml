name: Frontend Deploy

# Espelha o deploy.yml do backend. Em push a main: build + push da imagem para
# o GHCR e disparo de repository_dispatch no repo do backend (que faz o rollout
# health-gated). Em PR: só valida o build da imagem (sem push).

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "next.config.ts"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

env:
  IMAGE: ghcr.io/${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Login no GHCR
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (push fora de PRs)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.IMAGE }}:sha-${{ github.sha }}
            ${{ env.IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  notify-backend:
    runs-on: ubuntu-latest
    needs: build
    if: ${{ github.event_name == 'push' }}
    steps:
      # BACKEND_DISPATCH_TOKEN pode não estar configurado (ex.: secret ainda
      # não criado/reposto no repo). Sem esta guarda, a action rebenta com
      # "Parameter token or opts.auth is required" e falha o workflow inteiro
      # em TODOS os pushes a main, mesmo sem relação nenhuma com o commit.
      - name: Disparar rollout no repo do backend
        if: ${{ secrets.BACKEND_DISPATCH_TOKEN != '' }}
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.BACKEND_DISPATCH_TOKEN }}
          repository: tututazeni-spec/tututazeni-backend
          event-type: frontend-updated
          client-payload: '{"tag": "sha-${{ github.sha }}"}'

      - name: Aviso — secret em falta, rollout não disparado
        if: ${{ secrets.BACKEND_DISPATCH_TOKEN == '' }}
        run: echo "::warning::BACKEND_DISPATCH_TOKEN não está configurado neste repo — o rollout automático do backend NÃO foi disparado. Configura o secret em Settings > Secrets and variables > Actions para restaurar o comportamento normal."
